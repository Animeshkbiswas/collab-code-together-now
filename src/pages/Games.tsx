
import { Navbar } from '@/components/Navbar';
import { GamesList } from '@/components/GamesList';

const Games = () => {
  return (
    <div>
      <Navbar />
      <GamesList />
    </div>
  );
};

export default Games;
